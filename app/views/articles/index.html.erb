<div class="container">
  <% @articles.each do |article| %>
  <div class="row">
    <div class="article_preview">
    <h4><%= link_to article.title, article_path(article) %> </h4>
    <p><%= article.content %></p>
    </div>
  </div>
  <% end %>
</div>